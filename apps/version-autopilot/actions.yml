name: 'Greet Action'
description: 'Greet someone and log the time'
inputs:
  who-to-greet:
    description: 'The name of the person to greet'
    required: true
outputs:
  time:
    description: 'The time we greeted you'
runs:
  using: 'node20'
  main: 'dist/apps/action1/main.js'
